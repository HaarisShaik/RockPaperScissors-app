/* 
1. Add the required variables.
2. Hide the dice image.
3. Make all the score to zero (players, current).
4. Add an event listener to roll the dice.
    a) Generate the random number
    b) show the dice
    c) show the dice image
    d) Update the score if not 1
        1. Add to current score
        2. show the current score in UI
    e) if = 1
        1. change the active player
        2. Make current score to zero (variable)
        3. make the current 0,1 to zero in ui.
        4. Toggle the active class
5. Add event listener to hold button
    a) update the player score with the added current score and change it ui.
    b) check the winner
        a) if score >= target.
        b) change the name to winner.
        c) remove the active class and add the winner class.

6) Using state variable disable the roll dice and hold button.
*/
